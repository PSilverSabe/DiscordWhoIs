services:
  discordwhois:
    container_name: discordwhois
    image: ${DOCKER_REGISTRY-}discordwhois
    build:
      context: .
      dockerfile: DiscordWhoIs/Dockerfile
    env_file:
      - .env
    ports:
      - "8082:8080"
      - "8083:8081"
    restart: unless-stopped